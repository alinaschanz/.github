## what

one or two sentences: what changed and why.

## checks

- [ ] `python -m pytest -q` passes offline
- [ ] `ruff check .` is clean
- [ ] no new dependency, no api key, no paid endpoint
- [ ] labels / addresses / pool ids come with a source (link in this description)
- [ ] `data/` columns were only appended, never renamed (if touched)

## related

closes #
